apiVersion: v1
kind: Service
metadata:
  name: cartwish-backend-service
spec:
  selector:
    app: cartwish-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort
